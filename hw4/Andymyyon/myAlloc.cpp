#include <iostream>
#include <vector>

static size_t alocatedBytes = 0;
static size_t deallocatedBytes = 0;
static size_t currentlyUsedBytes = 0;

template <typename T>

class MyAlloc {
    
public:
    
};